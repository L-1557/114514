# Spring Boot Database Configuration

# H2 Database Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# H2 Database Configuration (Development)
spring.datasource.url=jdbc:h2:mem:waterdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MySQL Configuration (Production - Uncomment to use)
# spring.datasource.url=jdbc:mysql://localhost:3306/water_approval?useSSL=false&serverTimezone=UTC&characterEncoding=utf-8
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=your_password
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# PostgreSQL Configuration (Production - Uncomment to use)
# spring.datasource.url=jdbc:postgresql://localhost:5432/water_approval
# spring.datasource.driverClassName=org.postgresql.Driver
# spring.datasource.username=postgres
# spring.datasource.password=your_password
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Python AI Service Configuration
python.ai.service.url=http://localhost:8000

# Server Configuration
server.port=8080
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
